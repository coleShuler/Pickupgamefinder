
  # Pickup Game Finder

  This is a code bundle for Pickup Game Finder. The original project is available at https://www.figma.com/design/2zI4i7Qbo3z0LzjwwwilDP/Pickup-Game-Finder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  